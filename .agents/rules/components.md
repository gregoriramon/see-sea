---
trigger: always_on
---

*Always the "ionic generate component" must executed with  --standalone --spec=false
*Always components shoul be created in src/app/shared/components/
*Always when a component its .html is saved , the list of ionic/icons imports in ".ts" files should be revised according to its template and addIcons must be used